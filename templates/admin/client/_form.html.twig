{{ form_start(form) }}
    {% if app.request.attributes.get('_route') == 'app_admin_client_edit' %}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    {% endif %}
{{ form_end(form) }}
